name: Dataset OVERWRITE (Midnight Reset)

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - run: pip install huggingface_hub pandas

      - name: Run OVERWRITE
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python pipeline/update_dataset.py OVERWRITE
